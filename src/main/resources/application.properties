spring.application.name=mcs

# SQL Export Configuration
export.queries[0].sheet-name=Clients
export.queries[0].sql=SELECT 'Alice' as Name, 'USA' as Country UNION SELECT 'Bob', 'UK'
export.queries[1].sheet-name=Orders
export.queries[1].sql=SELECT 101 as ID, 'Laptop' as Item UNION SELECT 102, 'Phone'

# Email Configuration
export.email.to=krishnagaddam@example.com
export.email.subject=Your Excel Export
export.email.body=Attached is the generated report.

# Spring Mail (Mock/Dev)
spring.mail.host=localhost
spring.mail.port=1025
